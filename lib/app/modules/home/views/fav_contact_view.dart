import 'package:flutter/material.dart';

import 'package:get/get.dart';

class FavContactView extends GetView {
  const FavContactView({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        child: ListView(),
      ),
    );
  }
}
